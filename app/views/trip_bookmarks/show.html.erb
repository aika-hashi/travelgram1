<div class='my-imgs'>
<tr>
 <b> <div class='my-title'> 観光スポットお気に入り一覧</div>
   
      <div class='my-imgs-content'>
  
    <% @tripbookmark.each do |tripbookmark| %>
      
        <br>投稿ユーザー名:<%= tripbookmark.trip_tweet.user.nickname %>
        
          <%= link_to trip_tweet_path(tripbookmark.trip_tweet) do %>
          <br>  観光スポット名:<%= tripbookmark.trip_tweet.title %>
          <% end %>
        </td>
         <td> 
            <% tripbookmark.trip_tweet.images.each do |image| %>
             <%= link_to trip_tweet_path(tripbookmark.trip_tweet) do %>
         <br> <%= image_tag image ,class: 'my-images'   %> 
         <% end %>
          <% end %>
      
      <% end %>
      </tr>
    
    
  </div>
</div>

<%=link_to 'もどる', user_path(current_user.id), class:"back-btn1" %>
 
